// Ensures that the code inside it runs only after the HTML document is fully loaded
$(document).ready(function () {
// The css() function is then used to change the text color to red
  $('header').css('color', '#FF0000');
});
